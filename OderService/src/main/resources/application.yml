spring:
  application:
    name: OrderService
  datasource:
    url: jdbc:mysql://localhost:3306/OrderServiceMicroserviceDB?createDatabaseIfNotExist=true
    username: hamza
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: mysql

server:
  port: 8083