spring:
  application:
    name: ProductService
  datasource:
    url: jdbc:mysql://localhost:3306/ProductServiceMicroserviceDB?createDatabaseIfNotExist=true
    username: hamza
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: mysql

server:
  port: 8082